# SR3: Cursor Rules

## Scope

Global agent behavioral constraints via `.cursor/rules/` and `AGENTS.md`.

## Delivered

| Rule | Scope |
|------|-------|
| `rust-core-standards.mdc` | `**/*.rs` |
| `rust-architecture.mdc` | always |
| `slint-ui.mdc` | `**/*.slint`, `ui_application/**` |
| `rpi-workflow.mdc` | always |
| `agent-response-format.mdc` | always |

Plus `AGENTS.md` with skill roster and multi-agent roles.

## Validation

Rules appear in Cursor rule picker. `AGENTS.md` documents Executor/Validator/Critic workflow.
